# AI 테크니컬 라이팅 및 문서 작성 규칙 (Tech Writing Guideline)

본 규칙은 네이버 D2, 토스 테크(Toss Tech), 라인 엔지니어링, 넷플릭스 테크 블로그, 우버 엔지니어링(Uber Engineering)의 글쓰기 원칙을 기반으로 합니다.  
기계적이고 과장된 'AI스러운 표현'을 일체 배제하고, 현업에서 문제를 직접 마주하고 튜닝한 엔지니어/DBA의 담백하고 신뢰성 있는 어조를 유지합니다.

---

## 1. 보이스톤 및 문체 (Tone & Style)
1. **담백한 엔지니어 설명체**:
   - 문장 종결 어미는 `~다`, `~했다`, `~때문이다`, `~는 점이다`, `~하는 편이다`를 기본으로 한다.
   - 불필요한 장식성 미사여구나 번역투(`~에 있어`, `~를 통해`, `~라 할 수 있습니다`)를 쓰지 않고 단문 위주로 작성한다.
2. **기술 용어 원문 영어 표기 원칙 (★ 핵심)**:
   - `반납(Check-in)된 후 다시 할당(Check-out)`처럼 한국어와 영어를 괄호로 중복 병기하지 않는다. 둘 중 하나만 쓰되, **되도록 원문 영단어(Check-in, Check-out, Pin, Unpin, Roundtrip)**를 단독으로 사용한다.
   - 지나친 한글 음독("커넥션 풀", "배칭", "어레이 프로세싱", "라운드트립")을 지양하고, 원문 영단어(`Connection Pool`, `Batching`, `Array Processing`, `Roundtrip`, `Dirty Check`)를 그대로 표기한다.
3. **직관적인 쉬운 한국어 사용 (과도한 한자어 지양)**:
   - 여러 가지로 해석될 여지가 있거나 불필요하게 딱딱한 한자어 대신, 직관적이고 쉬운 표현을 쓴다.
   - 예시:
     - `괴리` → `차이점` (또는 `차이`)
     - `재대여` → `다시 할당`
     - `락 병목` → `Lock 쏠림` (또는 `Lock 경합`)
4. **문단 및 문서 내 표기 일관성 유지**:
   - 한 번 선택한 표기 방식(원문 영어 또는 한국어)은 해당 문단과 문서 내에서 일관되게 유지한다.
   - 같은 문단 내에서 한 줄은 `Java`, 다음 줄은 `자바`로 쓰거나, `Oracle`과 `오라클`, `DB`와 `데이터베이스`처럼 표기가 흔들리지 않도록 통일한다.
5. **동료에게 공유하듯 자연스러운 어조**:
   - 억지로 꾸며낸 감정이나 설득이 아닌, 실제 겪은 상황과 엔지니어링 관점의 사실과 판단 근거를 담담하게 적는다.
6. **구조적이고 아키텍처 중심의 시각 (Uber 스타일)**:
   - 개별 코드나 쿼리 단위의 튜닝에 머무르지 않고, 시스템 전체의 데이터 흐름, 부하 분산, 동시성 및 스케일 관점의 영향을 함께 조망한다.

---

## 2. 절대 금지 항목 (Banned Anti-Patterns)
1. **과장된 비장미와 자극적인 수식어 금지**:
   - `재앙`, `참사`, `비극`, `극강의 효율`, `눈부신 도약`, `파괴적인 안티패턴`, `마법 같은`, `선사합니다` 등 감정 과잉 단어 사용 금지.
   - 감정적 수식어 대신 구체적인 현상과 지표(Buffer Gets, Latency, Execution Count, P99/P95 응답 시간 등)로 객관화할 것.
2. **어설픈 한글 음독 및 중복 병기 금지**:
   - `반납(Check-in)`, `언핀(Unpin)` 식의 불필요한 괄호 병기 금지.
   - 기술 키워드를 한국어 발음대로 적는 표기("커넥션 풀", "배칭", "라운드트립") 금지 -> `Connection Pool`, `Batching`, `Roundtrip` 등 원문 영어 표기.
3. **인위적인 장식과 도식적 템플릿 금지**:
   - 글 서두에 `> "~~"` 식의 가짜 명언이나 감성 인용구 금지.
   - 천편일률적인 기계적 서식(통념 vs 현실 -> 핵심 메커니즘 -> 3대 재앙 -> 실행계획 -> 요약표 -> 체크리스트 -> 결론) 복붙 금지.
4. **독자를 훈계하거나 비꼬는 어조 금지**:
   - `산수를 해보면 본질이 보입니다`, `Starts에 속지 마십시오`, `소원대로 ~되었지만` 등의 어조 금지.
5. **공허한 동어반복 결론 금지**:
   - `이로써 시스템의 처리 성능을 극대화하고 안정적인 아키텍처를 구축할 수 있게 되었습니다` 같은 상투적 문구 금지.

---

## 3. 서사 구조 (Storyline)
1. **배경과 문제**: 실제 마주했던 운영/프로덕션 상황이나 실행 계획·트래픽 병목 상의 의문에서 출발.
2. **동작 원리 및 아키텍처 분석**: 겉으로 보이는 지표(Starts)와 내부 물리 동작(Buffer Cache, Random Access, Full Scan, 데이터 파이프라인 흐름)의 차이를 정확히 짚고, 기술적 선택의 트레이드오프를 객관적으로 분석.
3. **실측 데이터와 프로덕션 검증**: 실제 실행 계획(A-Plan), Buffers, Latency(P99/P95), 리소스 점유율 수치 비교로 입증.
4. **회고와 배운 점**: 롤아웃 시 주의점, 시스템적 한계, 작업하면서 직접 느낀 실무적인 교훈이나 주의사항으로 간결하게 마무리.

---

## 4. 아키텍처 및 시스템 도식화 규칙 (Architecture Diagram Guideline)
1. **텍스트 아스키 아트(ASCII Box Art) 전면 금지**:
   - `+---+`, `| ... |` 형태의 콘솔용 텍스트 상자 작성을 금지한다.
   - 데이터 구조, 블록 물리 배치, 컴포넌트 간 상호작용은 반드시 Docusaurus 호환 Mermaid 코드로 작성한다.
2. **좌우(LR) 데이터 파이프라인 흐름 지향**:
   - 세로(TD) 단순 나열보다, 유입(Source/Client) → 가공(Engine/Processor) → 영속(Storage/Buffer)의 흐름이 한눈에 보이는 `flowchart LR` 배치를 기본으로 사용한다.
3. **서브시스템 경계(Boundary) 분리**:
   - 모듈, 네트워크 경계, 계층(Layer)은 점선(`stroke-dasharray: 6 4`)의 `subgraph`로 명확히 격리한다.
4. **시맨틱 도형 및 파스텔 팔레트 표준 적용 (Uber/Toss 스타일)**:
   - 스토리지/DB/큐: 원통형 `[(...)]` + Green 계열 (`fill:#E8F5E9, stroke:#4CAF50, color:#1B5E20`)
   - 내부 연산/프로세서: 사각 박스 `[...]` + Yellow 계열 (`fill:#FFF9C4, stroke:#FBC02D, color:#5D4037`)
   - 외부 서비스/클라우드: 캡슐/클라우드 `(["..."])` + Blue 계열 (`fill:#E1F5FE, stroke:#0288D1, color:#01579B`)
   - 코어 엔티티/컨테이너: White/Card (`fill:#FFFFFF, stroke:#424242, color:#212121`)
5. **데이터 페이로드(Schema/Data) 및 캡션 명시**:
   - 화살표 연결선 위에는 단순 화살표 대신 전달되는 데이터 스키마 또는 작업(`|"[Neil, Sotto Mare]"|`, `|"Merge datasets"|`)을 표기한다.
   - 다이어그램 하단에는 반드시 `<p align="center"><em>Figure X: [설명]</em></p>` 또는 `[그림 X: 설명]` 캡션을 명시한다.
